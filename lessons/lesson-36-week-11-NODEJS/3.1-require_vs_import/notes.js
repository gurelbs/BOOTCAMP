const getNotes = () => 'Your Notes...'

// export default getNotes
const myName = () => 'gurel ben shabat'
const randomNumber = () => Math.floor(Math.random() * 100) + 1
const upper = x => console.log(x.toUpperCase());


export {
    getNotes,
    myName,
    randomNumber,
    upper
}