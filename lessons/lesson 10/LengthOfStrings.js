let  lengthOfStrings = arr => {
  for (let i = 0; i < arr.length; i++){
    console.log(arr[i].length);
  }
}

lengthOfStrings(["boo", "doooo", "hoo","ro"])
console.log(`\n`);
lengthOfStrings(["gurel", "ben", "shabat"])
